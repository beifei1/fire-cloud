spring:
  application:
    name: fire-user-consumer
  cloud:
    nacos:
      config:
        file-extension: yaml
        extension-configs:
          - data-id: fire-common-service-config.yml
            group: common
            refresh: true

      server-addr: http://192.144.176.31:80